import React from 'react';
import { render } from '@testing-library/react';
import $$component_name from '../$$component_name';

describe('test $$component_name component', () => {
  it('should render the correct default $$component_name', () => {

  });
});
