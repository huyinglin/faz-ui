import React from 'react';
import { render } from '@testing-library/react';
import Tree from '../Tree';

describe('test Tree component', () => {
  it('should render the correct default Tree', () => {

  });
});
