import styled from 'styled-components/macro';
import { themeColor } from '../../styled';

export const MessageContentView = styled.div`
  font-size: 12px;
  display: flex;
  align-items: center;
  align-content: center;
`;
