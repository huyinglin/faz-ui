import React from 'react';
import { render } from '@testing-library/react';
import Notification from '../Notification';

describe('test Notification component', () => {
  it('should render the correct default Notification', () => {

  });
});
