import React from 'react';
import { render } from '@testing-library/react';
import Carousel from '../Carousel';

describe('test Carousel component', () => {
  it('should render the correct default Carousel', () => {

  });
});
